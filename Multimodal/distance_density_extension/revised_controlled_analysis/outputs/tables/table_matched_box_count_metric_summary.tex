% Auto-generated from data/matched_box_count_pairs.csv by scripts/make_matched_box_count_tables.py
\begin{table}[t]
  \centering
  \caption{Direction of each metric across the 20 matched selected-box-count settings. The comparison is metric-specific: the lower distance-only threshold has the lower lost-ratio in every setting, while mAP@0.5 and native nuScenes mAP each favour the density gate in a small number of settings.}
  \label{tab:matched-box-count-metric-summary}
  \begin{tabular}{lccc}
    \toprule
    Metric & Distance-only & Density & Total \\
    \midrule
    Lost-ratio (lower is better) & 20 & 0 & 20 \\
    mAP@0.5 (higher is better) & 17 & 3 & 20 \\
    Native nuScenes mAP (higher is better) & 19 & 1 & 20 \\
    \bottomrule
  \end{tabular}
\end{table}
